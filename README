Builds up an array with global variable names, like 'alert', 'document', and 'scrollTo'

Try typing out a partial word and see the autocompletion below the text field, then try clicking the suggestions..
